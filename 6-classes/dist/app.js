"use strict";
class Department {
    // for initializing variables inside of the class
    constructor(n) {
        this.name = n;
    }
}
const it = new Department('IT'); //creating new object
console.log(it);
